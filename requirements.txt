# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20     # FastAPI 文件上传支持
websockets==14.1              # WebSocket 支持

# 兼容旧 Flask 路由（过渡期保留，可后续移除）
Flask==3.1.1
flask-cors

# OpenAI SDK
openai==1.84.0

# 日志
loguru==0.7.3

# 配置文件解析
PyYAML==6.0.2

# 图片处理
Pillow==12.0.0

# 请求库
requests==2.32.3

# 生产环境部署
gunicorn
gevent
