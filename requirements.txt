# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20     # FastAPI 文件上传支持
websockets==14.1              # WebSocket 支持

# 兼容旧 Flask 路由（过渡期保留，可后续移除）
Flask==3.1.1
flask-cors

# OpenAI SDK
openai==1.84.0

# 日志
loguru==0.7.3

# 配置文件解析
PyYAML==6.0.2

# 图片处理
Pillow==12.0.0

# 请求库
requests==2.32.3

# 声纹锚定（ECAPA，若环境不可用将自动回退到轻量 embedding）
torchaudio>=2.3.0
speechbrain>=1.0.2
# speechbrain 1.0.x 使用了已废弃的 use_auth_token 参数，需锁定 huggingface_hub<1.0
huggingface_hub>=0.23.0,<1.0.0

# 生产环境部署
gunicorn
gevent
